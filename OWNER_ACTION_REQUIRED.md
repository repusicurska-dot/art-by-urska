# Owner Action Required

## Artist's notes, Meaning & For the collector — AI-drafted, needs your review (important)

You asked me to imagine how you felt painting each piece and write it in a spiritual tone, so
`src/content/artworks.ts` now has real "artist's note", "Meaning", and "For the collector" text
for all 5 artworks instead of `[PENDING]` placeholders. **These are my drafts, not your real
words** — unlike the poems (verbatim from your `.docx` files), this text was generated by me at
your request as a starting point. Please read through all 5 artworks and either rewrite this
text in your own voice or approve it as final — it currently reads as if it's really you talking
about your own process, so it shouldn't go live unedited. It deliberately echoes the "soul
recognizes soul" language from your own `Backgrounds and me.docx`, and the artist's note section
links to `/spirituality` for continuity, per your request to connect the artwork pages to the
Spirituality section.

## Dark-fantasy homepage rebuild (done)

Rebuilt the homepage and global header/footer per your detailed spec, inspired by
oliviarosejames.com's layout principles (not its assets/copy/branding):

- New palette: `#030303` background, `#f2f0eb` text, `#8f8d88` muted — see `--color-ink`,
  `--color-bone`, `--color-smoke` in `globals.css`. Applied globally (header/footer everywhere),
  homepage + `/collection` fully, other inner pages (about/contact/legal/cart/checkout/poetry/
  spirituality) are still on the earlier light palette — flag if you want those converted too.
- Added `UnifrakturMaguntia` (blackletter/Gothic) via `next/font` for hero/section/category
  titles — `font-gothic` utility class.
- New sticky dark header (`SiteHeader.tsx`): monogram "AU", centered nav (Home/Gallery/Poetry/
  About — swapped your spec's "Membership" for "Poetry" since there's no membership system),
  cart bag icon with count, an inert "Account" icon (no login system exists, so it's
  non-functional — remove or wire up a real one later), thin announcement bar, full-screen
  black mobile menu.
- New cinematic hero (`CinematicHero.tsx`): 3-panel composition, center artwork dominant with
  dimmer side previews, autoplay (pauses on hover/tab-hidden), prev/next controls, slow zoom.
- New "Most Coveted" horizontal carousel (`FeaturedCarousel.tsx`) — all 5 artworks, swipeable,
  arrows + dots.
- New category banners (`CategoryBanners.tsx`): "Originals" → `/collection`, "Poetry Prints" →
  `/poetry`, "Commissions" → `/contact` (renamed from your spec's Prints/Originals/Commissions
  since those are the reference site's actual category names — used equivalents that map to
  real pages here).
- New reusable gallery lightbox (`components/shared/ArtworkLightbox.tsx`) with prev/next,
  arrow-key + Escape navigation, adjacent-image prefetch, scroll lock — wired into `/collection`.
- Removed the old homepage: the 5 bespoke poem-storytelling sections (split/fullscreen/quote-
  first/cinematic-macro/minimal) are no longer on the homepage — that full story experience
  (the real poems, "Her words" beats) still lives on each artwork's own detail page
  (`/artworks/[slug]`), reached via the hero, carousel, or category banners.
- Known gaps from the spec not fully done in this pass: full keyboard focus-trap inside the
  mobile menu overlay (Escape works, Tab could still reach elements behind it), AVIF/WebP format
  hints and blur placeholders on images (next/image optimizes automatically but no explicit
  `placeholder="blur"` added), and a full Lighthouse performance run.

## Premium polish pass (done)

- Cropped all 5 artwork photos to remove background clutter (shelf, floor, doily) visible at
  the edges of the original phone photos — full-bleed crops now show only the canvas.
- Shifted the whole site's color palette from gold/terracotta/sage to a monochrome
  black/white/silver system, per your request — each artwork now has its own grey accent
  instead of a color.
- Added a light "The Collection" interstitial between the homepage hero and the exhibition
  scroll, so two dark images no longer butt directly against each other.
- Rebuilt the artwork page opening: image and quote now sit side by side (not overlaid), and
  clicking an artwork image from the homepage opens a blurred-background lightbox with the full
  quote beside it before committing to the full page — inspired by the reference site you sent
  (oliviarosejames.com).
- Unified every primary button site-wide (`.btn-primary`/`.btn-secondary` in `globals.css`) with
  a consistent hover-lift + shadow instead of a flat color change, and added an animated
  underline to header nav links.
- Found and fixed a real bug: `AnimatePresence` (framer-motion) was silently failing to swap
  content in this React 19 setup — the price-reveal button looked broken. Replaced with a
  simpler pattern site-wide.

Not done in this pass (flag if you want a deeper round): exhaustive mobile-breakpoint testing,
image compression/optimization pass, accessibility contrast audit against the new grey palette,
loading-state polish on slow connections.


Everything below is needed from Urška before this site can launch. Nothing here has been
invented — these are the gaps left intentionally, on purpose, for her to fill in.

## Content

- [ ] **5 flagship artworks** (`src/content/artworks.ts`) now use your real titles, poems, and
      photography — both text and the embedded photo were pulled directly out of your `.docx`
      files in the project folder: "Blossoming Love", "The Prophecy", "Eternal Love", "Birds of
      Light", and "Somehow My Heart Still Remembers You" (photos now live in
      `public/images/`). If you revise those documents (new text or a better/updated photo), the
      site needs to be re-synced by hand (it's a snapshot, not a live link).
- [ ] You also have "ANGEL OF LIGHT.docx" and "ENCHANTED SWAN.docx" in the project folder —
      each has a real painting photo but is marked "NOT AVAILABLE" with no poem text, so
      neither is used on the site. If either becomes available, add the poem text and either
      swap it in for one of the 5 above or add it as a 6th/7th artwork (the content model
      supports this — see the README).
- [ ] For **all 5 artworks**: the "Meaning" and "For the collector" story beats, artist's note,
      year, medium, materials, dimensions, weight (if relevant), confirmed price
      (`priceConfirmed: true` once set), VAT/tax note, edition details, dispatch time, and any
      close-up/detail photography beyond the one photo already in each `.docx`.
- [ ] I paired each poem with its embedded photo by matching the imagery (e.g. "The Prophecy"'s
      dragon+knight photo, not the bride photo I'd originally guessed for it from an earlier
      session) — worth a quick sanity check that all 5 pairings are correct.
- [ ] The order of the 5 pieces (which layout/position each gets on the homepage) has no
      particular logic behind it — reorder freely via each artwork's `order` field in
      `artworks.ts`.
- [x] **About page** (`src/components/about/AboutContent.tsx`) — now uses your real bio and
      photos from `Backgrounds and me.docx` (climbing career → art, artist statement, 2 photos).
      No longer placeholder. If you want to add more of the studio/hands/sketches imagery the
      spec mentions, or swap in different photos from the same doc (there are 7 there in total,
      only 2 are used), just ask.
- [ ] Decide, per artwork, whether it's a standard original, made-to-order, or genuinely
      personalized/custom piece — this determines which withdrawal-rights section of the
      Returns policy applies to it.
- [ ] **Poetry section** (`/poetry`, `src/content/quotes.ts`) — new sibling section selling
      quote/poem prints as a lighter, more affordable product line alongside the paintings.
      Entirely placeholder right now (6 empty slots) per your instruction — needs real quote
      text, titles, and confirmed prices. Currently "inquire to purchase" only (via the contact
      form), not integrated into the cart/checkout flow — say if you want that added.
- [ ] **Spirituality page** (`/spirituality`) — reuses real lines from `Backgrounds and me.docx`
      (the "soul"/"art from my soul" statements) that weren't used on `/about`. Not placeholder,
      but it's my curation/ordering of your draft lines, not a finished piece you wrote as one —
      worth a read-through and edit in your own voice.
- [ ] Both new sections are only cross-linked from the footer ("More from Urška") for now, not
      in the main header nav — say if you want them promoted more prominently, or if/when we do
      the bigger "Art / Poetry / Climb" hub restructuring you mentioned.

## Business & legal facts (`src/content/business.ts`)

- [ ] Legal business name, legal form (s.p. / d.o.o. / etc.)
- [ ] Registered address
- [ ] Business registration number
- [ ] VAT/tax number
- [ ] Contact email (and data-protection contact, if different)
- [ ] Phone (if required)
- [ ] Returns address
- [ ] Bank details (if handling any payment/refund manually)
- [ ] Applicable ADR/ODR dispute-resolution contact, if any

## Legal pages — professional review required

Every page under `/legal/*` is structurally complete but explicitly flagged as "pending legal
review" on the page itself. Before launch:

- [ ] Have a qualified professional review Terms & Conditions, Privacy Policy, Cookie Policy,
      Shipping & Delivery, Returns & Cancellations, and the Legal Notice against actual
      Slovenian/EU law as it applies to this specific business.
- [ ] Confirm VAT treatment/registration and update the VAT note per artwork accordingly.
- [ ] Confirm which GPSR/product-safety disclosures actually apply and finalize the economic
      operator block on `/legal/notice`.
- [ ] Confirm the applicable consumer ADR/ODR body, if any.

## Shipping

- [ ] Real packaging method/materials
- [ ] Whether shipments are insured, and to what value
- [ ] Tracking provision and carrier
- [ ] Actual shipping costs per zone/artwork size (currently: "confirmed before payment", not a
      fixed number)

## Payments

- [ ] Choose and set up a payment provider (Stripe Checkout was the original plan — see
      `src/lib/payments.ts`). Implement `createCheckoutSession()` and add live API keys.
- [ ] Decide on a crypto payment option, if still wanted.

## Contact / email

- [ ] Choose an email or CRM provider and wire it into `POST /api/contact`
      (`src/app/api/contact/route.ts`) — inquiries are currently validated but not delivered
      anywhere.
- [ ] Set up transactional order-confirmation email once payments go live.

## Analytics / cookies

- [ ] Decide whether to add analytics (and which provider). The cookie-consent system
      (`src/lib/cookieConsent.ts`) is real and ready — any script added later must check
      `hasConsent("analytics" | "marketing")` before loading, and the Cookie Policy page should
      be updated to name it.

## Deployment

- [ ] Connect the repo to GitHub + a hosting provider (e.g. Vercel) under Urška's own account.
- [ ] Set `NEXT_PUBLIC_SITE_URL` to the real production domain (used by metadata, sitemap, and
      Open Graph tags).
- [ ] Turn off `NEXT_PUBLIC_SHOW_PLACEHOLDER_BANNER` before going live (or leave it — it's off
      by default).

## Final QA before launch

- [ ] Re-read every `[BRACKETED PLACEHOLDER]` on the live site once real content is in — search
      the codebase for `PENDING`, `TO BE CONFIRMED`, and `[` to find them all.
- [ ] Confirm no analytics/marketing script fires before consent (none exist yet, so this is
      currently trivially true — re-check once one is added).
- [ ] Full click-through of the purchase flow with live payments once Stripe is wired up.
